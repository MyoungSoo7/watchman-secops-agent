# Watchman(파수꾼) — 제출 문서

> NVIDIA 챌린지 제출용. 심사 4축 구조. 모든 수치는 **실측 근거**에 링크한다.
> 미달·미측정은 미달로 적는다(ROLE.md T4-2). 작성 2026-09-22, **수치 갱신 2026-09-23 23시(KST)**.
>
> ✅ **P7 확정(2026-09-22, 팀 브리핑 + 공식 링크):** **온라인 예선 = 자유 주제**, "NVIDIA Skill/API 활용
> 실작동 Agent"를 **9/28 온라인 신청서로 제출**, 심사 → **본선 10팀 선정**("미션 당일 공개"는 10/7 오프라인
> 본선). 지정교육 **DLI S-FX-43 수료 완료**, **"NVIDIA Build" = build.nvidia.com**(Watchman 이 이미 실호출).
> **자유 주제이므로 Watchman(SecOps) 그대로 적격.** 공개 rules 페이지 자체는 비공개(초청/로컬 추정) —
> 요건은 팀 브리핑으로 확정, 교육·플랫폼은 공식 링크로 1차 확인. 상세: [`eval/CHALLENGE-SOURCE-SEARCH.md`](eval/CHALLENGE-SOURCE-SEARCH.md).

## 한 줄 요약

**Alertmanager 알림을 받아 스스로 로그·리소스를 조사(read-only)하고 원인을 분류해
조치를 "제안"하는 in-cluster SecOps 에이전트.** 이 프로젝트의 본체는 에이전트 기능이
아니라 **에이전트 자체를 6축으로 잠근 보안 통제 설계**다 — "만든 것"과 "그걸 통제한
방법"을 한 세트로 보여준다. 자동 실행 없음. 전 스텝 감사 기록.

---

## 축 ① 기술 활용 심도 (NVIDIA·에이전트 기술)

- **NIM 실사용.** `nvidia/nemotron-3-super-120b-a12b` 를 `integrate.api.nvidia.com/v1`
  (OpenAI 호환 chat/completions)로 호출. 실 알림을 분류·조사한 완주 run 이 감사로그에 존재
  (예: `20260920-201123-001`, `20260921-150400-003`). 과부하 대응: 429·5xx 에 **최대 5회 시도,
  `Retry-After` 우선·지수 백오프(5·10·20·40s+지터), 동시 호출 2개 제한**(2026-09-23 배포, 재시도는 stdout 로그).
- **진짜 에이전트 루프.** 단발 프롬프트가 아니라 관측→판단 다단계 루프: `es_search`(ES 로그)·
  `kube_read`(K8s 리소스) 도구를 LLM 이 골라 호출하고, 그 관측 위에서 `finish` 로 분류·제안.
  스텝 예산 6(통제 ③). 대체가설(오탐 회의) 강제 프롬프트로 과잉경보 억제.
- **NVIDIA 실행 기록 연결.** `/state`(FR-15)가 run 별 모델 ID·LLM 호출 수·프롬프트/완료
  토큰·소요시간을 집계. 라이브 스냅샷 발췌(in-cluster 파드, 실 NIM, 2026-09-22):

  | run_id | ns | model | llm_calls | prompt_tok | completion_tok | duration_s | 결과 |
  |---|---|---|---|---|---|---|---|
  | 20260922-102851-003 | sparta-prod | nemotron-3-super-120b | 6 | 7876 | 2076 | 63.2 | 완료 · 주입 `injection_suspects=2`(⚠) |
  | 20260922-102450-002 | agent-system | nemotron-3-super-120b | 6 | 12683 | 1798 | 56.2 | 완료 · confidence 중간 |
  | 20260922-102310-001 | monitoring | nemotron-3-super-120b | 4 | 7294 | 717 | — | **실패(NIM 503 재시도 소진)** — 크래시 없이 격리 |
  | **합계** | — | — | **16** | **27,853** | **4,591** | — | 완료 2 · 실패 1 |

  **누적 운영 실측 (감사로그 전량, 2026-09-21 03:55 ~ 09-23 22:57 KST, in-cluster 파드에서 집계):**

  | 항목 | 값 |
  |---|---|
  | 유입 알림 | **317** (Falco 285 · Alertmanager 32) |
  | NIM 응답(`llm_out`) | **1,457** 회 |
  | 도구 호출(`es_search`·`kube_read`) | **1,042** 회 · 인자 검증 거부 `arg_rejected` 120 (대부분 LLM 형식 오류, 허용목록 밖 리소스 요청 6) |
  | 조사 완주(`finish`) / 부분 결과(`finish_partial`) | 253 / 56 |
  | 카드 발송 / 억제 / 발송 실패 | 291 / 7 / 0 |
  | LLM 실패(`llm_error`) | **54** — 전부 NIM 측 `HTTP 503` 28 · `HTTP 429` 26 (고지 7) |
  | 신뢰도 분포(누적) | 높음 22 · 중간 162 · 낮음 125 |

  원천: 파드 안 `/data/audit.jsonl` 의 `kind` 별 집계 + `GET /state` 의 `totals`.
  (토큰 수는 감사로그에 없어 재기동 전 run 은 복원되지 않는다 — 토큰 표는 아래 발췌 run 한정.)

  전체 표·환경·재현은 **[`eval/NVIDIA-USAGE.md`](eval/NVIDIA-USAGE.md)**. 실패 run 은 크래시가
  아니라 503 재시도를 소진한 뒤 격리한 **회복 동작**(ROADMAP 리스크 #1의 실제 완화). 당시엔 3회 재시도였고
  2026-09-23 에 5회+백오프로 강화했다. 데모 세션(2026-09-24 00시) run 10건의 실행표는 같은 문서 §2.1 —
  완료 3 · 부분 결과 6(전부 NIM 429/503) · 복구 필요 1 을 그대로 실었다.
- **Build Skill API 어댑터 — 실호출 기록 보유.** 기본 OFF 어댑터(`skill_query` 도구 +
  `skill_call` 감사기록)를 심어두고, `NVIDIA_SKILL_ENABLED=1` 로 켠 상태에서 **실호출 1건을
  감사로그에 실제로 남겼다**: `skill_call` → endpoint `integrate.api.nvidia.com/v1/chat/completions`,
  prompt 46 / completion 1024 tok(원문 `eval/skill-call-20260922.jsonl`). OFF 상태에선
  프롬프트·도구 집합이 기존과 byte-identical(회귀 0). → 축 ④.
  ⚠ 정직: 이 호출은 검증된 NIM 엔드포인트를 향한다. "Build Skill" 이 별도 제품/필수 요건인지는
  P7 미확정 — env 3줄로 공식 엔드포인트 교체 가능하게 설계(코드 무변경).

## 축 ② 실용성·산업가치·혁신성

- **실제 운영 클러스터에 상주.** 6노드 K3s(Lemuel) `agent-system` 네임스페이스에서 실
  Alertmanager webhook 을 받는다. 데모용 노트북이 아니라 서비스.
- **실무 문제를 푼다.** 새벽 알림의 1차 트리아지 — "이게 진짜 장애인가, stale/노드재부팅
  동반 오탐인가"를 로그·리소스 근거와 함께 3초 카드로. 실측 사례: 존재하지 않는 cron
  Job 을 "stale"로, 재시작 중인 canary 를 "정상 오탐"으로 정확히 분류(P4 §2).
- **혁신 포인트 = 안전한 에이전트.** 대부분의 "AI 운영 자동화"가 실행 권한을 주는 방향인데,
  Watchman 은 **의도적으로 실행을 빼고 제안+감사+승인 게이트**만 남겼다. SecOps 에서
  신뢰의 전제조건. 선행 사례(NVISO cyber-security-llm-agents 등)와의 차별점은
  "실클러스터 상주 + 에이전트 자체 통제 6축"(SPEC §9).
- **랜섬웨어 대응 4층 (2026-09-23, 전부 운영 반영).** 단발 알림 분류를 넘어 공격의 *흐름*을 본다.
  - **킬체인 상관관계(FR-21, `chain.py`)** — 180분 창 안에서 서로 다른 단계 2개 이상이면 승격, 카드에 단계 경로.
    결정론 판정(LLM 아님). 탐지 룰명으로 확인된 단계가 1개 이상일 때만 최고 심각도, **어휘로만 잡힌 체인은
    '의심'(warning)** 에 머문다 — 공격자가 cmdline 에 단어를 심어 승격을 유도하는 걸 막는다.
  - **출력 유출 통제(FR-22, `redact.py`)** — 카드·메일·감사로그로 나가는 문자열을 송신 직전 마스킹. 값은 반환하지 않음.
  - **복구가능성 조사(FR-23, `recovery.py`)** — velero 백업을 읽어 복구 가능/지연/불가 의심/미확인 판정.
    **조회 실패는 '정상'이 아니라 '미확인'.**
  - **정기 인바리언트(FR-24, `invariants.py`)** — 알람이 없을 때의 구조적 결함을 매일 09시 KST 에 점검
    (자격증명 위치·버킷 잠금·백업 암호 키·원격데스크톱 리스너·백업 신선도). 첫 운영 실행 결과는 PASS 2 · UNKNOWN 3 (고지 6).
- **Falco 런타임 탐지 연동.** 유입 알림의 90%(285/317)가 Falco. 같은 룰·호스트·컨테이너·실행파일·부모의 반복은
  120분 창으로 묶되 **emergency·alert·critical·error 는 절대 묶지 않는다**(9/23 배포, 운영 실적은 아직 0건). Falco 는 ArgoCD 앱으로 관리.
- **부수 채널 이중화.** 텔레그램 카드 + 이메일(best-effort) 동시 발송. 한 채널 죽어도 도달.

## 축 ③ 완성도

E2E 가 **이미 돈다**: 실 알림 → 조사 → 분류 → 카드. 그 위에 통제·평가·가시화를 실측으로 채움.

- **보안 통제 6축, 각각 라이브 실측 증거 보유** (원문 출력: **[`eval/CONTROL-MATRIX.md`](eval/CONTROL-MATRIX.md)** — 2026-09-22 클러스터 직접 측정):

  | 축 | 통제 | 라이브 실측 결과 |
  |---|---|---|
  | ① | 주입 감지(⚠) | 실 페이로드 주입 → run `injection_suspects=2`, 에이전트 복종 없이 `완료` |
  | ② | 공개호스트 쓰기 가드 | `POST /alert` → `HTTP 403` "public endpoint is read-only" |
  | ③ | 이그레스(포트·CIDR 허용목록) | 파드에서 `:80`·`:8080` → `ConnectionRefusedError` 차단, `:443` OPEN(NIM/SMTP 의도 허용) |
  | ④ | 자동 실행 없음 | 등록 도구 `es_search`·`kube_read`(읽기)뿐, 산출은 제안 카드만 |
  | ⑤ | 레드팀 감지 | 12/12 감지·오탐 0, `run_redteam.py` exit 0 |
  | ⑥ | 비루트·readOnlyRootFS | `uid=65534(nobody)`, 루트FS 쓰기 `Read-only file system`, `drop:[ALL]` |

  > 정직 기재: ③은 "외부 전면 차단"이 아니라 포트·목적지 허용목록(외부 443/587은 NIM·SMTP용 의도 허용).
  > 2026-09-23 인바리언트 I4 용으로 노드망 TCP 3389/3390 을 추가로 열었다(접속 시도로 리스너 유무만 보고 데이터는 주고받지 않음, SPEC FR-12). ④의 승인·실행 게이트(M3)는 미구현 — 실행 경로 자체가 없어 자동 변경 불가. 상세·원문은 CONTROL-MATRIX.md.

- **평가 1회전 완료 (P4):** 파이프라인 계약 충족 **10/10**(mock, 결정적) + 분류 정확도
  **4/4**(실 NIM, 정답 확정분). 하네스 `eval/run_eval.py`·`eval/score_cases.py` 재현 가능.
  ⚠ 4/4 는 합성 편향 표본 — 제품 정답률로 인용하지 않음(P4 §3 정직 기재).
- **레드팀 결과표 (P5):** 코드 감지층 **12/12 = 100%**, 오탐 **0/12**. `eval/run_redteam.py`
  가 exit 0 로 회귀 게이트. LLM 거부층은 샘플 2건 실측(전수 라이브는 NIM 안정 구간 후속).
- **가시화:** `GET /state`(FR-15) JSON — 최근 run·상태 7값·사용량 집계. 주입 시 카드 ⚠ 배지.
  공개 호스트(`security.lemuel.co.kr`)는 POST 403 으로 읽기 전용 강제.
- **테스트:** 단위테스트 **119건 전부 통과** — `test_watchman` 85 · `test_security_layers` 23 · `test_card_evidence` 11
  (2026-09-23 재실행).
- **회귀 게이트 하네스 6종, 전부 exit 0** (2026-09-23 재실행, 클러스터·LLM 없이 결정론적으로 재현):

  | 하네스 | 채점 대상 | 결과 |
  |---|---|---|
  | `run_eval.py` | 파이프라인 계약(제어 흐름·출력·주입 오탐) | 10/10 |
  | `run_redteam.py` | 주입 감지층 | 감지 12/12 · 오탐 0/12 |
  | `run_chain.py` | 킬체인 승격 시점 + 오탐 A~E(정상 뱅크·단계 반복·창 밖·주입 어휘·어휘 전용 체인) | 전부 통과 |
  | `run_egress.py` | 출력 유출(마스킹·원문 잔존·정상 오탐) | 전부 통과 |
  | `run_recovery.py` | 복구 판정(조회 실패 ≠ 정상) | 전부 통과 |
  | `run_invariants.py` | 인바리언트 판정('없다' ≠ '모른다', 출력 비밀값 0) | 전부 통과 |

## 축 ④ 커스터마이징·독창성

- **stdlib-only, 외부 의존성 0.** 공급망 최소화 자체가 통제 ①의 일부. `python:3.12-slim` 위에 모듈 5개
  (`watchman.py`·`chain.py`·`redact.py`·`recovery.py`·`invariants.py`)를 ConfigMap 으로 얹어 돈다 — 빌드할 이미지가 없다.
- **위협 모델을 코드로.** "최대 공격면은 ES 로그 내용(간접 프롬프트 주입)"이라는 판단에서
  전 통제를 역산: ⑤(무력화 시도) + ①④(성공해도 할 게 없음) + ⑥(했다면 남는다) 다층.
- **레드팀 뱅크가 실제로 구멍을 드러냈다.** 초안 4패턴은 3/12 만 잡았다. 뱅크가 exfil
  역방향·도구 오용·경로 탈출·한국어 역할탈취 우회를 드러내 패턴을 4+1 추가 → 12/12.
  "감지율 자랑이 아니라 구멍 발견"이 뱅크의 존재 이유(P5 §2).
- **확장성 설계 = Build Skill API OFF 어댑터.** 런타임을 안 건드리고 미래 요건에 선제 대응.
  게이트 하나로 조사 도구가 늘고 호출이 감사로그에 남는다 — 독창적 리스크 관리.

---

## 정직 고지 (미달·미측정·미완)

1. **분류 정답률 4/4 는 합성 편향 소표본.** 실알림 3건 라벨 미확정, NIM 503 미완주 2건 존재.
   케이스 뱅크 10건 in-cluster 분류 재생은 NIM 안정 구간 후속(P4 §5).
2. **레드팀 LLM 거부층은 전수 라이브 미완**(샘플 2건만). 코드 감지층만 12/12 확정.
3. **P7 — 확정(2026-09-22).** 요건이 팀 브리핑으로 확정됐다: **온라인 예선은 자유 주제**, "NVIDIA
   Skill/API 활용 실작동 Agent"를 9/28 온라인 폼으로 제출, 심사 → 본선 10팀. **"NVIDIA Build" =
   build.nvidia.com** 이고 Watchman 은 이미 그 추론 엔드포인트(`integrate.api.nvidia.com`)로 실호출 중이며,
   그 위에 **공식 Skill 로 패키징해 SkillEvaluator Tier-1(정적·구조 검증) 통과**(키 없이 재현, `PASS · exit 0`,
   6검증기 0 errors, 관찰 1건 medium — `skills/watchman-secops-triage/`). 97.8/A 는 SKILL.md 문서 구조 품질
   점수이지 모델·에이전트 성능이 아니며, Tier 2·3 는 미실행이다. 지정교육 **DLI S-FX-43 수료 완료**.
   자유 주제라 Watchman 그대로 적격 — 남은 것은 빌드가 아니라 **제출**(데모 영상·온라인 폼). 다만 공개
   rules 페이지 자체는 공개 웹에 인덱싱돼 있지 않아(초청/한국 로컬 추정) 요건 텍스트의 출처 등급은
   "팀 브리핑(내부 공식)"이다(`eval/CHALLENGE-SOURCE-SEARCH.md`).
4. **S1 관제 뷰**는 SHOULD(별도 진행). 없어도 제출 성립 — `/state` JSON 을 문서에 노출.
5. 통제 축 명칭의 NemoClaw 공식 축과 1:1 매핑은 코스 자료 대조 후 확정(SPEC §6 주).
6. **정기 인바리언트(P9) 5항목 중 3항목은 운영에서 "미확인(UNKNOWN)" 으로 답한다 — 의도된 한계.**
   매일 09시 KST 실측(2026-09-23 첫 가동): I4 원격데스크톱·I5 백업 신선도는 PASS 로 실측되지만,
   - I1 백업 자격증명: 클러스터 *안* 에 있다는 것까지만 확인. R2 토큰의 삭제 권한 범위는 API 로 안 보인다.
     (2026-09-22 수동 점검 기준으로는 결함이다 — 자동 판정이 그걸 못 본다.)
   - I2 오프사이트 버킷 잠금: watchman 에 R2 조회 토큰이 없다.
   - I3 백업 암호화 키: Secret 을 읽어야 하는데 watchman 은 **secrets 무권한** 이 원칙이라 조회를 시도조차 않는다
     (`INVARIANT_REPO_SECRET_READ=0`. 켜면 다이제스트만 남기는 코드는 준비돼 있다).
   조회 실패를 PASS 로 읽지 않는 것이 설계 원칙이다. 이 셋을 실측으로 바꾸는 일(velero·R2 백업 경로 보강)은
   **미래 과제** 로 남긴다(ROADMAP STRETCH).
7. **NIM 호출 실패 54건(유입 317 대비 약 17%) — 전부 NIM 측 과부하(503 28 · 429 26).** 9/22~23 Falco 폭주
   구간(하루 알림 108~203건)에 몰렸다. 실패한 run 도 크래시 없이 **'부분 결과' 카드**(수집된 근거까지 + 실패 사유 명시,
   신뢰도 낮음)로 나가서 **경보 자체는 끊기지 않았다**(카드 발송 실패 0). 2026-09-23 18시 백오프·동시성 제한 배포 이후에도 **지속되는 429 는 백오프로 흡수되지 않았다** —
   2026-09-24 00시 데모 세션에서 run 10건 중 6건이 5회 재시도(대기 78–81s)를 소진하고 부분 결과로 끝났다
   (`eval/NVIDIA-USAGE.md` §2.1). 백오프가 버티는 건 순간 과부하까지이고, NIM 측 용량·호출 한도 자체는 우리가 바꿀 수 없다.
8. **데모 픽스처가 실제 카드를 오염시켰다 — 발견 당일 수정.** 2026-09-24 데모에서 `fx-` 픽스처 알림이 킬체인 상관 링버퍼에
   실제 알림과 함께 들어가, 실제 Falco 카드 2장에 가짜 "랜섬웨어 3/4단계" 줄이 붙어 **운영자에게 실제로 발송됐다**
   (카드 전송 차단은 `fx-` 를 봤지만 상관 단계는 안 봤다). 픽스처 전용 링으로 분리하고 회귀 테스트 2건을 더해 배포했으며
   (`d88ff56`), 배포 뒤 재실행에서 픽스처 체인이 실제 알림 없이 2단계로만 조립되는 것을 확인했다.
   같은 날 두 번째 결함도 고쳤다: 창 안에 체인이 있으면 단계와 무관한 알림에도 체인 줄이 붙었다.
   이제 체인 줄은 그 알림 자신이 체인의 한 단계일 때만 붙는다(회귀 테스트 2건).

## 부록 — 재현 명령

```bash
python3 -m unittest test_watchman test_security_layers test_card_evidence   # 85+23+11 = 119 통과
python3 eval/run_eval.py                      # 파이프라인 10/10 (mock, 결정적)
python3 eval/run_redteam.py                   # 감지 12/12, 오탐 0 → exit 0
python3 eval/run_chain.py && python3 eval/run_egress.py && python3 eval/run_recovery.py && python3 eval/run_invariants.py  # 랜섬웨어 4층, 전부 exit 0
python3 eval/score_cases.py <감사로그> eval/runs-labels-20260922.json  # 실 run 분류 채점
```

관련 문서: [SPEC.md](SPEC.md) · [ROADMAP.md](ROADMAP.md) · [ROLE.md](ROLE.md) ·
[docs/architecture.md](docs/architecture.md) · [eval/REPORT.md](eval/REPORT.md) ·
[eval/REDTEAM.md](eval/REDTEAM.md) · [eval/evidence-P1-P2-P3-S2.md](eval/evidence-P1-P2-P3-S2.md)
